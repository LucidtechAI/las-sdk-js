{
  "name": "@lucidtech/las-sdk",
  "homepage": "https://lucidtech.ai",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "upgrade-lucidtech": "lerna run --sort tsc && lerna bootstrap --sort && yarn upgrade --scope @lucidtech && lerna run --sort tsc",
    "watch": "lerna run watch",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "compile": "lerna run compile",
    "recompile": "lerna clean -y && lerna run compile",
    "build": "lerna clean -y && lerna run compile && jest"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/jsdom": "^12.2.4",
    "@types/node": "^12.7.2",
    "@types/uuid": "^3.4.5",
    "html-webpack-plugin": "^3.2.0",
    "jest": "^24.9.0",
    "jsdom": "^15.1.1",
    "lerna": "^3.16.4",
    "ts-jest": "^24.0.2",
    "ts-loader": "^6.0.4",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3",
    "uuid": "^3.3.3",
    "webpack": "^4.39.3",
    "webpack-cli": "^3.3.7"
  }
}
